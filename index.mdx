---
title: CoW Protocol Contracts
description: Smart contracts and TypeScript SDK for Gnosis Protocol v2, enabling batch auction trading with on-chain settlement
mode: custom
---

<div className="relative bg-gradient-to-b from-[#0f1117] via-[#1a1d27] to-[#0f1117] dark:from-[#0f1117] dark:via-[#1a1d27] dark:to-[#0f1117] py-20 overflow-hidden">
  <div className="max-w-6xl mx-auto px-6 relative z-10">
    <div className="text-center">
      <h1 className="text-5xl md:text-6xl font-bold dark:text-gray-100 text-gray-900 mb-6">
        CoW Protocol Contracts
      </h1>
      <p className="text-lg md:text-xl dark:text-gray-400 text-gray-600 max-w-3xl mx-auto mb-8">
        Production-ready smart contracts and TypeScript SDK for building on Gnosis Protocol v2. Create batch auction orders, integrate with solvers, and settle trades on-chain across multiple networks.
      </p>
      <div className="flex flex-wrap gap-4 justify-center">
        <a
          href="/quickstart"
          className="px-6 py-3 bg-[#f2a91a] hover:bg-[#EAA621] text-black font-semibold rounded-lg transition-colors"
        >
          Get started
        </a>
        <a
          href="/api/ts/order"
          className="px-6 py-3 border border-gray-300 dark:border-[#27272a] bg-white/10 dark:bg-white/10 hover:bg-white/20 dark:hover:bg-white/20 dark:text-gray-100 text-gray-900 font-semibold rounded-lg transition-colors"
        >
          Explore API
        </a>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Quick start</h2>
  <p className="dark:text-gray-400 text-gray-600 mb-8">Get up and running with CoW Protocol in minutes</p>
  
  <Steps>
    <Step title="Install the package">
      Install the CoW Protocol contracts package via npm or yarn:
      
      <CodeGroup>
      ```bash npm
      npm install @cowprotocol/contracts
      ```
      
      ```bash yarn
      yarn add @cowprotocol/contracts
      ```
      
      ```bash pnpm
      pnpm add @cowprotocol/contracts
      ```
      </CodeGroup>
    </Step>
    
    <Step title="Create an order">
      Import the SDK and create a signed order:
      
      ```typescript
      import { Order, domain, signOrder, computeOrderUid } from "@cowprotocol/contracts";
      import { ethers } from "ethers";

      const order: Order = {
        sellToken: "0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2",
        buyToken: "0x6B175474E89094C44Da98b954EedeAC495271d0F",
        receiver: ethers.constants.AddressZero,
        sellAmount: ethers.utils.parseEther("1.0"),
        buyAmount: ethers.utils.parseEther("1000"),
        validTo: Math.floor(Date.now() / 1000) + 3600,
        appData: ethers.constants.HashZero,
        feeAmount: ethers.utils.parseEther("0.01"),
        kind: "sell",
        partiallyFillable: false,
      };

      const settlementContract = "0x9008D19f58AAbD9eD0D60971565AA8510560ab41";
      const chainId = 1;
      
      const signature = await signOrder(
        domain(chainId, settlementContract),
        order,
        signer,
        "eip712"
      );
      ```
    </Step>
    
    <Step title="Submit to the protocol">
      Submit your signed order to the CoW Protocol API for settlement:
      
      ```typescript
      const orderUid = computeOrderUid(domain(chainId, settlementContract), order, signer.address);
      
      // Submit to CoW Protocol API
      const response = await fetch("https://api.cow.fi/mainnet/api/v1/orders", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          ...order,
          signature,
          signingScheme: "eip712",
          from: await signer.getAddress(),
        }),
      });
      ```
      
      <Note>
        Your order will be included in the next batch auction and settled by solvers when a favorable match is found.
      </Note>
    </Step>
  </Steps>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Explore by topic</h2>
  <p className="dark:text-gray-400 text-gray-600 mb-8">Deep dive into CoW Protocol's architecture and capabilities</p>
  
  <CardGroup cols={2}>
    <Card
      title="Core concepts"
      icon="book-open"
      href="/concepts/architecture"
    >
      Understand batch auctions, settlements, orders, and the solver network
    </Card>
    <Card
      title="Smart contracts"
      icon="file-contract"
      href="/contracts/overview"
    >
      Explore the settlement contract, vault relayer, and authentication system
    </Card>
    <Card
      title="TypeScript SDK"
      icon="code"
      href="/sdk/overview"
    >
      Learn to create orders, sign messages, and encode settlements
    </Card>
    <Card
      title="Multi-chain deployments"
      icon="globe"
      href="/deployment/networks"
    >
      Access contract addresses across Ethereum, Gnosis, Arbitrum, and more
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-3xl font-bold dark:text-white text-gray-900 mb-4">Key features</h2>
  <p className="dark:text-gray-400 text-gray-600 mb-8">Everything you need to build on CoW Protocol</p>
  
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div className="dark:bg-[#1a1d27] bg-gray-50 p-6 rounded-lg dark:border dark:border-[#27272a] border border-gray-200">
      <div className="text-[#f2a91a] mb-3">
        <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>
      </div>
      <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Batch auction settlement</h3>
      <p className="dark:text-gray-400 text-gray-600">Execute multiple orders in a single transaction with optimal price discovery through competitive solver auctions</p>
    </div>
    
    <div className="dark:bg-[#1a1d27] bg-gray-50 p-6 rounded-lg dark:border dark:border-[#27272a] border border-gray-200">
      <div className="text-[#f2a91a] mb-3">
        <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
        </svg>
      </div>
      <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">EIP-712 order signing</h3>
      <p className="dark:text-gray-400 text-gray-600">Secure order creation with typed data signing, supporting multiple signature schemes including EIP-1271</p>
    </div>
    
    <div className="dark:bg-[#1a1d27] bg-gray-50 p-6 rounded-lg dark:border dark:border-[#27272a] border border-gray-200">
      <div className="text-[#f2a91a] mb-3">
        <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
        </svg>
      </div>
      <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Multi-chain support</h3>
      <p className="dark:text-gray-400 text-gray-600">Deployed on 8+ networks including Ethereum, Gnosis Chain, Arbitrum, Optimism, Base, Polygon, BSC, and Avalanche</p>
    </div>
    
    <div className="dark:bg-[#1a1d27] bg-gray-50 p-6 rounded-lg dark:border dark:border-[#27272a] border border-gray-200">
      <div className="text-[#f2a91a] mb-3">
        <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4" />
        </svg>
      </div>
      <h3 className="text-lg font-semibold dark:text-white text-gray-900 mb-2">Flexible order types</h3>
      <p className="dark:text-gray-400 text-gray-600">Support for fill-or-kill, partial fills, buy/sell orders, and custom balance management via Balancer Vault</p>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="dark:bg-[#1a1d27] bg-gray-50 dark:border dark:border-[#27272a] border border-gray-200 rounded-2xl p-8 text-center">
    <h2 className="text-2xl font-bold dark:text-white text-gray-900 mb-3">Ready to build?</h2>
    <p className="dark:text-gray-400 text-gray-600 mb-6 max-w-2xl mx-auto">
      Start integrating CoW Protocol into your application today. Follow our quickstart guide to create your first order in minutes.
    </p>
    <a
      href="/quickstart"
      className="inline-block px-6 py-3 bg-[#f2a91a] hover:bg-[#EAA621] text-black font-semibold rounded-lg transition-colors"
    >
      Get started now
    </a>
  </div>
</div>
